# Example environment configuration for integrating schema-tools

# OpenSearch/Elasticsearch URLs
OPENSEARCH_URL=http://localhost:9200
ELASTICSEARCH_URL=http://localhost:9200

# Schema path (relative to your project root)
SCHEMAS_PATH=db/schemas

# Optional: Build/deployment identifiers
BUILD_NUMBER=123
DEPLOYMENT_ID=myapp-v1.2.3